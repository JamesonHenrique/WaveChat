package com.jhcs.wavechat.domain.enums;

/**
 * Enumeração que representa o estado de uma mensagem.
 */
public enum MessageState {
    /**
     * Estado indicando que a mensagem foi enviada.
     */
    SENT,

    /**
     * Estado indicando que a mensagem foi vista.
     */
    SEEN
}